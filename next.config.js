/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add this if you're using experimental features
  experimental: {
    // Remove any Next.js 15 experimental features
  }
}

module.exports = nextConfig